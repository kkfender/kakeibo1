   
  

<div class="center-block">
 
  <%#="月の予算#{@users.monthly_budget}円"%>
 <%#="今月の出費#{@with_sum[@users.id]}円"%>
 <%#="今月の入金#{@depo_sum[@users.id]}円"%>


  <div class="btn-group btn-group-justified"role="group">
  <div class="btn btn-default "><%=link_to "#{@this_month.last_month.strftime("%Y年%m月")}",
  "/outputs/index/#{@this_month.last_month}",role:"button"%>
  </div>
   <div class="btn btn-default"><%=@this_month.strftime("%m月")%></div>
   
    <div class="btn btn-default"><%=link_to "#{@this_month.next_month.strftime("%Y年%m月")}",
    "/outputs/index/#{ @this_month.next_month}",role:"button"%>
   </div>
   
</div> 
            
            <h1 class="text-right"><%="出金額#{@with_sum[@users.id]}円"%></h1>
  <table class="table table-borderd table-striped table-hover">
    <tr><th>出金日</th><th>店名</th><th>品目</th><th>出金額</th><th>入金額</th><th>メモ</th><th>編集</th></tr>
 
  <% @outputs.each do |output| %>
  <%if output.user_id==@users.id &&%> 
  <tr>
  <td><%=output.date%></td><td><%=output.shop_name%></td><td>
    <%=output.category%></td><td><%=output.withdrawal%></td><td><%=output.deposit%></td>
    <td><%=output.memo%></td>
    <td><%=link_to '編集',"/outputs/#{output.id}/edit"%></td>
  </tr>
    <%end%> 
    <%end%>
    
  </table>
  </div>
  
  <%= paginate @outputs %>